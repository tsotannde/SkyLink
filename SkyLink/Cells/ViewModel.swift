struct ViewModel
    {
        let flagImage: UIImage?
        let city: String
        let state: String?
        let totalCapacity: Int
        let currentPeers: Int
        let showCrown: Bool
    }